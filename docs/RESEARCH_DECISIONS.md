# Research Decisions Log

## Phase 3.6c pre-evaluation decision (2026-08-30)

- Phase 3.6c is the final automatic threshold-refinement iteration. It is justified by the frozen
  Phase 3.6b finding that the best combined strict-preserving rule remained precision-perfect on
  development (`precision=1.0000`) but recall-limited (`recall=0.6795`, weighted F1 `0.8282`,
  `TN=42, FP=0, FN=25, TP=53`). Its remaining false negatives comprised 6 partial-overlap, 14
  correct-document/incomplete-evidence, and 5 wrong-document-retrieval cases.
- Before any Phase 3.6c candidate outcome is calculated, the terminal rule is frozen: a candidate
  must achieve ordinary automatic-sufficient precision at least 0.90 and prevalence-weighted F1
  at least 0.85. Neither gate may be weakened. If no member of the predeclared expanded grid
  reaches both gates, automatic label refinement stops. No further threshold search, pretrained
  model, or automatic rescue iteration may be launched autonomously; a new methodological
  decision is required.
- Phase 3.6c remains DEVELOPMENT-only. It preserves every strict positive, uses only existing
  strict-span/coverage and selected-model NLI aggregates, excludes benchmark-impossible and
  semantic-unevaluable cases from primary selection, and forbids TEST information, retrieval
  strategy, retrieval depth, identifiers, and split provenance as rescue-rule inputs.

## Phase 3.6c completed development outcome (2026-08-30)

- The expanded grid was frozen before outcome calculation at canonical SHA-256
  `253caf6047cd1516161e572d566b30419a6006228e8658185bdb4359e5b1d83d`. It contains 77 total
  candidates: 74 new settings and three exact Phase 3.6b comparison points. No NLI inference or
  new model was used.
- The selected combined strict-preserving rule uses `T_cov=0.20`, `T_mean=0.35`, `T_min=0.05`,
  and selected-premise contradiction `<0.50`. On the 120-row primary DEVELOPMENT set it obtained
  precision 0.9833 (95% Wilson CI 0.9114-0.9971), recall 0.7564, raw F1 0.8551, and prevalence-
  weighted F1 0.8622 (`TN=41, FP=1, FN=19, TP=59`). It therefore passed both unchanged gates.
  The Wilson interval is descriptive and did not create another gate.
- The final target configuration SHA-256 is
  `5b9a394e5e97776844054f3282af815462148adc62b528c77245d61707406977`. It mechanically preserves
  all 1,686 strict positives and rescues 1,116 additional positives, producing 2,802 final
  positives and 3,354 final negatives across 6,156 eligible TRAIN+VALIDATION conditions from 513
  questions. The 24 semantic-unevaluable conditions from DEV_Q066 and TRAIN_Q526 remain NA and
  excluded; benchmark-impossible and unresolved-evidence cases remain excluded.
- A final 50-row/50-question blinded confirmation pack was frozen after target selection. It has
  zero overlap with the original 150 development questions and the superseded 100-question
  semantic-only sample, and contains zero benchmark-impossible, semantic-unevaluable, or TEST
  rows. After both question-disjoint exclusions, all 293 remaining eligible questions are in the
  train split and no validation question remains; the resulting all-train allocation is therefore
  the only feasible split allocation. Strategy, depth, and final-label categories remain closely
  balanced. Human fields are blank and the answer key remains sealed.
- Independent confirmation gates remain precision at least 0.90 and prevalence-weighted F1 at
  least 0.85. Recall, raw F1, accuracy, confusion matrix, ambiguous rate, and appropriate Wilson or
  bootstrap intervals will also be reported. No second genuine human annotator is required;
  unavailable inter-annotator agreement remains a stated limitation. Phase 4 remains blocked
  pending this confirmation.

## Phase 3.6b decisions (2026-08-30)

- Corrected the two human-annotation filenames atomically so their paths match the frozen
  provenance record. `phase03_annotation_annotator_1.csv` now names the original byte-identical
  86-sufficient/64-insufficient file (DEV_Q217 sufficient; SHA-256
  `04c0f33db4ca3ac9d2a58322f6399c1688ef66a48855bbb7c7546ca7d5851959`).
  `phase03_annotation_annotator_1_adjudicated.csv` now names the reviewed byte-identical
  85-sufficient/65-insufficient file (DEV_Q217 insufficient; SHA-256
  `3e7a87f2da694cbb40930c142549cd70dde86c0d89429885711549cf91b99cc4`). This was a
  filename/provenance correction only: neither CSV was rewritten and no annotation value was
  altered by the correction. The original file remains the historical frozen Phase 3.5/3.6
  development annotation; only the adjudicated file is the Phase 3.6b development input.
- Recorded that the original 150 labels were subsequently reviewed against the already frozen
  material-completeness definition. One borderline label, blind order 6 / DEV_Q217, changed from
  sufficient to insufficient after an AI-assisted review of the evidence. This was not a second
  human annotator and is not inter-annotator validation. The sole changed row is benchmark-
  impossible and is excluded from PRIMARY rule selection.
- Before evaluating the adjudicated labels, froze the exact 59-candidate strict-preserving rescue
  grid at canonical SHA-256
  `4112de919a857992a52c077d54d7c64594778c1c9732eb37be69990c17709f78`. Every candidate retains
  strict positives; the grid contains 5 coverage-only, 9 NLI-only, and 45 combined candidates.
  Retrieval strategy, depth, identifiers, split, and benchmark-impossible status are not rescue
  boundary predictors. The 0.90 ordinary-precision and 0.85 weighted-F1 gates are unchanged.
- The semantic-only 100-row confirmation pack is marked `superseded_unannotated`. Its answer-key
  values were not opened. The pre-result 50-question replacement-sample decision is retained as a
  study limitation but can be applied only if the development gates pass.
- No candidate passed both development gates. The selected eligible combined rule (`T_cov=0.50`,
  `T_mean=0.45`, `T_min=0.10`, contradiction `<0.50`) obtained precision 1.0000, recall 0.6795,
  F1 0.8092, and prevalence-weighted F1 0.8282. Phase 3.6b therefore stops with
  `PHASE_03_6B_DECISION_REQUEST.md`; no final target, replacement confirmation sample, TEST
  aggregate, or Phase 4 work is permitted from this result.

Record every methodological decision that could change results.

| Date | Decision | Alternatives considered | Rationale | Affects test set? |
|---|---|---|---|---|
| 2026-08-17 | Use a custom TechQA connected-component research split targeting approximately 70/15/15. Link questions by shared gold/source filename or identical normalized-question hash; assign whole components while optimizing total-size and answerability-class balance. Retain `TRAIN_Q`/`DEV_Q` only as provenance. Persist component IDs and assignments, then freeze and reuse them after validation. | Original ID-prefix partition; row-stratified random split; filename-only groups; duplicate-question-only groups. | The released dataset exposes one Hugging Face split, while ID-prefix partitions share source files and duplicate questions. Connected components prevent direct and transitive source/duplicate leakage. | Yes — approved before the research test split is generated or inspected. |
| 2026-08-17 | Define `y_suff(q,C)` at the actual retrieved-chunk bundle: positive only when at least one complete admissible evidence set needed for the reference answer is covered by the retrieved chunk text. A correct-document hit is recorded separately and does not imply sufficiency. | Global benchmark answerability; correct-filename retrieval; retrieval-score threshold; whole-document availability. | The classifier must predict retrieval-conditioned evidence sufficiency, not question-level answerability or document retrieval success. | Yes — label construction and all downstream evaluations use this frozen distinction. |
| 2026-08-17 | Use an operational two-stage primary policy with hybrid retrieval: start at `k=5`; medium confidence requests additional retrieval evidence by expanding once to `k=10`, recomputing the frozen feature vector and sufficiency probability; answer if the expanded score is sufficient under the frozen high threshold, otherwise abstain. Do not iterate beyond `k=10` or describe expansion as a user request. | Treat request-more-evidence as a terminal user-facing class; ask the user for evidence; arbitrary repeated retrieval; no expansion. | This gives the middle decision a reproducible system action and tests whether additional retrieved evidence can safely recover coverage. | Yes — thresholds are selected on validation and the frozen policy is applied once to test. |
| 2026-08-17 | Make the full risk-coverage relationship and AURC, with unsafe-answer/coverage trade-offs, the primary selective-prediction evaluation. Predeclare validation selective-risk constraints of 5%, 10%, and 20% as secondary operating points; use 10% only as the central illustration. | A single primary 10% constraint; arbitrary fixed thresholds; using selective risk and unsafe-answer rate interchangeably. | No single risk tolerance is universally acceptable. Full curves expose safety-coverage behaviour, while fixed constraints provide transparent sensitivity points. | Yes — metrics, operating constraints, and threshold selection are fixed before test. |
| 2026-08-17 | Use explicit versioned sufficiency and grounding annotation guidelines and target approximately 120 independently double-annotated examples for each applicable validation sample, within the approved 100-150 range. Report raw percentage agreement and Cohen's kappa before adjudication. If a second annotator is unavailable, store agreement statistics as NA and report the limitation. | Single annotation only; double-annotate a small convenience subset; infer or omit unavailable agreement statistics. | Independent agreement quantifies categorical judgement reliability while retaining a valid, transparently limited protocol if staffing prevents double annotation. | Yes — sampling and reporting rules are predeclared; test labels cannot alter them. |
| 2026-08-17 | Initialize benchmark-impossible TechQA conditions as context-sufficiency negatives with provenance `benchmark_impossible_initialization`, and manually audit a stratified anomaly-focused subset. Never initialize benchmark-answerable conditions as sufficient; label them from their actual retrieved chunks. | Discard impossible rows; use global answerability as the target for every row; assume all answerable rows are sufficient. | The benchmark supplies no supported evidence for impossible rows, but possible annotation anomalies must remain auditable and global answerability cannot establish retrieved-context sufficiency. | Yes — provenance, audit, and allowed override rules are frozen before test evaluation. |
| 2026-08-17 | Explicitly flag answerable TechQA rows `DEV_Q014` and `DEV_Q094` with empty references. Persist a manual official-corpus alignment only if unambiguous; otherwise exclude them only from analyses requiring gold retrieval/context-sufficiency evidence and report the exclusion. Never fabricate an evidence location. | Treat empty text as evidence; mark the questions impossible; drop the rows globally; infer a likely source span. | These rows cannot support defensible gold-evidence metrics until evidence is established, but remain valid dataset/split records and may remain eligible for analyses not requiring gold evidence. | Yes — eligibility and exclusions are determined by the frozen evidence protocol, not test performance. |
| 2026-08-18 | Generate and persist Phase 2 retrieval rankings for train, validation, and test questions, but restrict Phase 2 aggregate development retrieval metrics and reports to train and validation. Do not calculate, display, rank, interpret, or include aggregate test retrieval metrics during Phase 2. Produce test retrieval metrics only in the final frozen evaluation workflow after retrieval, sufficiency, model, calibration, and policy decisions are fixed. Test rankings may be checked only for non-performance integrity such as schema, counts, uniqueness, rank continuity, prefixes, and hashes. | Omit test rankings until final evaluation; calculate test retrieval aggregates during Phase 2; expose test aggregates but promise not to tune from them. | Later phases require the exact persisted test bundles, while hiding aggregate test performance prevents retriever/chunking/parameter adaptation to test outcomes. Integrity validation can prove completeness without exposing performance. | Yes — this seals aggregate test retrieval outcomes before any full retrieval run. |

| 2026-08-18 | Before any retrieval run, replace the prototype's 360-word/80-word-overlap chunks with deterministic fast-tokenizer windows from the pinned MiniLM revision: 224 content tokens, 48-token overlap, 176-token step, `add_special_tokens=False`, exact offset mappings, and source-exact substrings of the normalized retrieval document. Persist raw and normalized source hashes, token/character offsets, chunk hashes, and canonical provenance-derived chunk IDs. | Retain the prototype word windows; use smaller word windows; tokenize after word chunking. | MiniLM accepts at most 256 word pieces. A 360-word chunk could be silently truncated by dense encoding while BM25 indexed all its text, invalidating the shared-corpus control. Tokenizer-bounded chunks eliminate that asymmetry and preserve auditable offsets. This amendment was approved before retrieval rankings or test results existed. | Yes - amended and frozen before any retrieval/test result was produced. |
| 2026-08-18 | Freeze BM25 as `rank_bm25.BM25Okapi(k1=1.5,b=0.75,epsilon=0.25)` with tested NFKC+casefold preprocessing and regex `(?u)\w+(?:[./:-]+\w+)*`, identically for corpus and query, without stop words, stemming, or lemmatization. Pin the package version and do not tune parameters. | Prototype ASCII regex; generic word tokenizer; dense tokenizer terms; another BM25 implementation/variant. | The tested Unicode regex preserves practical technical identifiers and internal separators while making term boundaries and BM25 implementation semantics reproducible. | Yes - fixed before retrieval and not selected from validation/test outcomes. |
| 2026-08-18 | Freeze dense retrieval to symmetric, prefix-free encoding with pinned `sentence-transformers/all-MiniLM-L6-v2` revision, CPU float32 normalized embeddings, and exact blocked NumPy dot product; load the dimension from the model. Freeze hybrid to equal-weight RRF over BM25 top 100 and dense top 100, constant 60, final depth 10, with ascending chunk-ID tie breaks and persisted constituent ranks/scores. | Query/passage prefixes; GPU or reduced precision; ANN; full-corpus or other RRF candidate depths; tuned fusion. | Symmetric exact CPU search avoids instruction and approximation confounds. A declared candidate pool makes RRF executable and auditable; no parameter is selected using validation or test results. | Yes - fixed before retrieval and test outcomes remain sealed. |
| 2026-08-18 | Serialize research-critical Parquet artifacts through an exact pinned PyArrow version after canonical-key sorting, with stable column order, Parquet 2.6, ZSTD level 9, dictionary encoding disabled, statistics enabled, data-page 1.0, and 65,536-row groups. Persist physical-file and canonical-logical semantic SHA-256 hashes; semantic equality is primary and physical equality is also checked in the pinned environment. | Backend-dependent pandas Parquet; canonical JSONL only; default writer settings. | Explicit writer settings and dual hashes separate scientific content reproducibility from physical encoding while still detecting byte drift in the canonical environment. | No direct metric effect, but it governs reproducibility of all split artifacts including sealed test rankings. |

| 2026-08-18 | Resume the validated partial MiniLM embedding checkpoint on the unchanged canonical CPU/float32/PyTorch stack despite the measured approximately five-hour remaining runtime. Preserve every frozen retrieval parameter and checkpoint after every batch. | Stop Phase 2; change to GPU; change the embedding backend, dtype, model, or chunk corpus. | Long CPU execution was explicitly approved after feasibility evidence was recorded. Continuing the identical deterministic method avoids introducing a device/backend confound and reuses only integrity-validated completed batches. | No - no query ranking or test performance existed when continuation was approved. |

| 2026-08-19 | Use complete coverage of any defensibly aligned confirmed-gold answer span by the union of the actual retrieved chunk intervals as the primary automatic `y_suff(q,r,k)` rule. Align by literal source match first, then by the frozen reversible textual normalization; retain every defensible occurrence and never choose one duplicate arbitrarily. Correct-document retrieval alone remains insufficient. | Global answerability; correct-document hit; single-chunk containment; semantic similarity, fuzzy matching, NLI, or LLM-generated spans; arbitrary selection among duplicate occurrences. | TechQA's original annotation protocol selected the shortest contiguous span sufficient to answer the question and excluded questions requiring multiple separate spans or multiple Technotes. Complete span coverage is therefore a defensible, auditable primary rule tied to the actual retrieved bundle. This was frozen before constructing labels or examining aggregate test alignment/label outcomes. | Yes - the same mechanical rule may later create sealed test rows, but test aggregates cannot refine it. |
| 2026-08-19 | Treat TechQA `is_impossible` as benchmark-relative provenance: Phase 3 assigns distinct preliminary benchmark-negative condition rows, not an ontological assertion that the complete 28,481-document corpus contains no relevant evidence. Preserve these rows so Phase 4 can run a preregistered sensitivity analysis excluding benchmark-impossible negatives without rebuilding labels. | Treat impossible as universally unsupported; discard impossible questions; merge them indistinguishably with evidence-aligned negatives. | Original impossibility means no answer was found in the benchmark candidate Technotes, while this study retrieves from the complete TechQA-RAG-Eval corpus. Distinct provenance prevents overclaiming and supports contamination auditing and the required downstream sensitivity. | Yes - interpretation and Phase 4 sensitivity are fixed before model performance is observed. |
| 2026-08-19 | Prepare one seeded, approximately 150-condition, question-disjoint, blinded manual validation pack from train/validation only, stratified across automatic outcomes, boundary/error categories, retrievers, depths, and splits. Keep the automatic answer key separate. Support two independent human annotators but report agreement and Cohen's kappa as pending when annotations are unavailable. | Draw from test; expose automatic labels; fabricate LLM annotations; require completed human annotation before producing automatic Phase 3 artifacts. | The pack validates the deterministic rule without contaminating test or substituting machine judgement for human annotation. Separate blinded material prevents anchoring by the automatic label. | No test examples are manually inspected during Phase 3 development. |
| 2026-08-19 | Freeze the Phase 3 human-validation analysis before opening annotations. The 150-condition audit has equal allocation (30 each) across five exclusive strata, so unweighted overall accuracy is descriptive only and not a natural-population estimate. Report stratum metrics, non-ambiguous automatic-label precision/recall/F1, ambiguity separately, disagreement categories, and an aggregate derived from stratum confusion rates weighted by actual resolved TRAIN+VALIDATION frequencies. Predeclare internal gates of sufficient-label precision >=0.90 and prevalence-weighted F1 >=0.85. If >10% of non-ambiguous audited benchmark-impossible conditions are human-judged sufficient, exclude those preliminary negatives from PRIMARY Phase 4 training and retain them only for sensitivity analysis. Target approximately 100 independently double-annotated conditions; Cohen's kappa >=0.80 is desirable, while lower agreement requires disclosed analysis/adjudication. This approximately-100 overlap supersedes the earlier approximately-120 target for the Phase 3 sufficiency audit only; it does not alter the later grounding-annotation protocol. | Report unweighted sample accuracy as population accuracy; post-hoc gates; omit ambiguous cases; treat a machine judge as a second annotator; require all 150 cases to be double annotated. | Equal-allocation stratification changes sample prevalence. Frozen weighting and gates prevent misleading population claims and result-contingent validity criteria, while the overlap target balances reliability assessment with practical staffing. | Yes - fixed before human responses or aggregate TEST outcomes are examined; only TRAIN+VALIDATION frequencies enter weighting. |
| 2026-08-29 | Interpret the completed strict gold-span-coverage rule as `strict_span_sufficiency`: a retained high-precision but low-recall proxy, not the primary context-sufficiency ground truth. Preserve its artifacts and use it for verified positives, diagnostics, and semantic-method comparison. The observed strict-rule metrics were precision 1.0000, recall 0.3488, F1 0.5172, and prevalence-weighted F1 0.5642, so the frozen 0.85 weighted-F1 gate failed and remains unchanged. | Lower/remove the gate; proceed with the strict rule; overwrite strict labels; spot-correct the 56 sampled disagreements. | The one-sided 56-false-negative pattern spans every automatic-negative stratum and empirically demonstrates systematic under-identification of semantically sufficient contexts. Preserving the strict result retains provenance without misrepresenting it as ground truth. | Yes - Phase 4 is blocked until a revised semantic label independently passes the unchanged gates; no TEST aggregate was inspected. |
| 2026-08-29 | Permanently exclude benchmark-impossible rows from the PRIMARY Phase 4 classifier-training population because the frozen human audit found 8/30 (26.67%) context-sufficient, above the strict >10% trigger. Retain them only for sensitivity analysis, a separate benchmark-impossible analysis, and robustness discussion. | Keep them as primary negatives; relabel all as sufficient; discard them entirely. | The benchmark annotation is relative to candidate Technotes and is contaminated as a negative label in the full-corpus retrieval setting. The action was frozen before human results and cannot be reversed based on later model performance. | Yes - fixes the primary training population before modelling and leaves TEST sealed. |
| 2026-08-29 | Run Phase 3.6 as offline semantic label refinement. Treat the original 150 human annotations as development-only; freeze the three NLI candidates (`cross-encoder/nli-deberta-v3-base`, `MoritzLaurer/DeBERTa-v3-base-mnli-fever-anli`, `facebook/bart-large-mnli`), deterministic claim/context handling, and finite threshold search before comparing development performance. Select safety-first (precision >=0.90, then maximum prevalence-weighted F1, then recall). After freezing, draw a new non-overlapping 100-condition blinded answerable-only TRAIN+VALIDATION confirmation sample and retain the unchanged 0.90 precision and 0.85 weighted-F1 confirmation gates. | Reuse the 150 as final validation; use a proprietary judge; add candidates after results; inspect TEST; proceed directly to Phase 4. | Development/final-confirmation separation prevents optimistic reuse of labels that shaped the semantic rule. Local pinned NLI provides reproducible offline support scoring while keeping all gold-derived information out of runtime features. | Yes - the revised primary target is not approved until independent confirmation passes; TEST remains sealed. |
| 2026-08-29 | Amend only Phase 3.6 execution mechanics before any candidate metric existed: use CPU inference batches of 16 and append-only raw NLI score checkpoints so interrupted runs resume exactly. The initial batch-8 process was terminated by the execution host before candidate 1 completed. The interval was reduced from 256 to 64 after a host recycle occurred inside Candidate 2's first block, then to 16 when the slower candidate still could not commit a 64-pair block within 13 minutes. No Candidate-2 score or candidate metric had been persisted. Models, revisions, claim segmentation, context units, NLI probabilities, threshold grid, gates, and selection objective are unchanged. | Continue runs whose large uncommitted blocks are repeatedly lost; reduce the candidate set; alter sequence length or semantic thresholds; use an external API. | Checkpointing is required for reproducible completion on the measured CPU-only Windows environment. Batch size and checkpoint interval change throughput and interruption loss, not the declared scientific estimand or decision rule; no development metric had been produced or inspected. | No TEST data or result is involved; exact execution configuration is hashed. |
| 2026-08-29 | Before inspecting any candidate performance metric, replace the infeasible all-single/all-adjacent-pair context-unit expansion with one deterministic claim-ranked multi-chunk window per reference claim. Sentence/list snippets from every retrieved chunk enter lexical relevance ranking; select at most three snippets with distinct-chunk priority, stable source/rank ties, and at most two snippets per chunk; render at 256 tokens using equal per-snippet head/tail budgets. The superseded raw Candidate-1 and partial Candidate-2 pair-score caches are excluded by the amended base-config hash and are not development results. | Continue a procedure requiring 110,041 unique NLI forwards for the 6,180-row primary population on the measured two-thread CPU; drop cross-chunk evidence; truncate only the earliest chunks; reduce the candidate set after timing it. | A results-blind feasibility count and Candidate-2 throughput showed that the original expansion required weeks on the frozen host. The amendment still considers every chunk for relevance, preserves source text, supports cross-chunk synthesis, and reduces development scoring from 4,410 to one pair per deterministic claim (503 answerable-development claims). It was frozen for all candidates before any human-linked candidate metric was calculated or inspected. | No TEST row, aggregate, or outcome was loaded; candidate models, revisions, thresholds, gates, and selection objective remain unchanged. |
| 2026-08-30 | Apply a general semantic-unevaluable rule when any unchanged frozen-segmentation reference claim exceeds the selected NLI model's valid pair-input budget. Preserve strict labels, set every condition-level `y_suff_semantic` for that question to NA with status `unevaluable` and reason `claim_exceeds_frozen_nli_pair_budget`, retain token-budget metadata, and exclude the question from primary semantic-target training/calibration/evaluation and confirmation sampling. Keep the scoring configuration SHA `98f7279821921d825470ee64efa810777e5b331d4c978e6234e7b689b6657fdf` unchanged and hash this policy separately as label governance. The mechanical rule currently affects two TRAIN+VALIDATION questions/24 conditions. | Truncate claims; introduce post-selection claim segmentation; force the rows negative; hard-code the two observed question IDs. | Truncation can remove evidence-bearing content, while new segmentation would alter the selected procedure. Explicit NA avoids inventing a semantic class, preserves strict diagnostics, and transparently accounts for a very small affected population. The decision was made after the input-budget guard fired but before semantic-label generation, confirmation sampling, Phase 4, or TEST evaluation. | No TEST semantic score or aggregate was calculated; the same frozen rule must be applied mechanically in any later sealed workflow. |

Do not retroactively alter a decision after viewing test results without recording it as a post-hoc change.
